import 'package:flutter/material.dart';

class Redeems extends StatelessWidget {
  const Redeems({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('redeems'),
    );
  }
}